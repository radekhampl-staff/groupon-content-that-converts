pandas>=2.0.0
numpy>=1.24.0

# Install ONE of the following depending on your LLM provider:
anthropic>=0.40.0      # for LLM_PROVIDER=anthropic (default)
# openai>=1.30.0       # for LLM_PROVIDER=openai
